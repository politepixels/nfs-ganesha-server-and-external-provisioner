apiVersion: v1
kind: ConfigMap
metadata:
  name: nfs-ganesha-config
  namespace: {{ .Release.Namespace }}
data:
  config: |-
    exports:
{{- range .Values.nfsExports }}
      - path: {{ .path }}
        squash: {{ .squash }}
{{- end }}
